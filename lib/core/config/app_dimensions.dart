import 'package:flutter/material.dart';

/// Application-wide spacing and sizing constants.
class AppDimensions {
  AppDimensions._();

  // Padding
  static const double defaultHorizontalPadding = 16;
  static const double defaultVerticalPadding = 10;

  // Radius
  static const double radiusSmall = 8;
  static const double radiusMedium = 12;
  static const double radiusLarge = 24;
  static const double radiusFull = 999;

  // Heights
  static const double buttonHeight = 52;
  static const double inputHeight = 52;
  static const double appBarHeight = kToolbarHeight;

  // Sizes
  static const double iconButtonDimension = 40;
  static const double progressLoaderDimension = 40;
}